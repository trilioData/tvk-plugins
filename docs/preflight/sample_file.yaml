run:
  namespace: default
  storageClass: csi-gce-pd
  cleanupOnFailure: true
  pvcStorageRequest: 2Gi
  resources:
    requests:
      memory: 64Mi
      cpu: 250m
    limits:
      memory: 128Mi
      cpu: 500m

cleanup:
  namespace: default
  logLevel: info
  cleanupMode: all
